option_settings:
  # Use Nginx as the reverse proxy server
  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: nginx
  # The name of or path to the main application file
  aws:elasticbeanstalk:container:python:
    WSGIPath: application:application
    NumProcesses: 1
    NumThreads: 15